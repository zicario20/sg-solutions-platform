import { describe, expect, it, vi } from "vitest";
import { createConfiguredClientServicesRuntime } from "../../apps/app/src/lib/client-services/configured-runtime";
describe("M009 configured runtime", () => { it("fails closed without admission/auth/source configuration", async () => { const runtime = createConfiguredClientServicesRuntime(); expect(runtime.query).toBeUndefined(); expect(await runtime.admit("client_services_ssr", new Request("https://atlas.test/client/services"))).toBe(false); }); it("uses an admitted configured source without synthetic fallback", async () => { const admission = { admit: vi.fn().mockResolvedValue(true) }; const runtime = createConfiguredClientServicesRuntime({ admission, authPort: { authorize: vi.fn(), revalidate: vi.fn(), selectContext: vi.fn() } as never, source: {} as never }); expect(runtime.query).toBeDefined(); expect(await runtime.admit("client_services_list_get", new Request("https://atlas.test/api/client/services"))).toBe(true); }); });
