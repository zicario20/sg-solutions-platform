import{describe,expect,it}from"vitest";import{CLIENT_SERVICE_LIST_LIMIT,parseClientServiceDetailDto,parseClientServiceListDto,serializeClientServiceSection}from"@atlas/client-services";import{M009_TEST_NOW,syntheticM009Card}from"./fixtures";const card=syntheticM009Card(),empty={state:"empty",generatedAt:M009_TEST_NOW.toISOString()}as const;describe("M009 public contracts",()=>{it("accepts bounded v2 DTOs and rejects internal fields",()=>{expect(parseClientServiceListDto({schemaVersion:"m009.list.v2",context:card.context,items:[card]}).items).toHaveLength(1);expect(()=>parseClientServiceListDto({schemaVersion:"m009.list.v2",context:card.context,items:[{...card,internalId:7}]})).toThrow();expect(()=>parseClientServiceListDto({schemaVersion:"m009.list.v2",context:card.context,items:Array.from({length:CLIENT_SERVICE_LIST_LIMIT+1},()=>card)})).toThrow()});it("strips data from non-fresh sections",()=>{const sections={timeline:{state:"fresh",generatedAt:M009_TEST_NOW.toISOString(),data:[{label:"Started"}]},tasks:empty,documents:empty,payments:empty,appointments:empty,messages:empty,agreements:empty,deliverables:empty};expect(parseClientServiceDetailDto({schemaVersion:"m009.detail.v2",context:card.context,service:card,scopeLabel:"Accepted scope",milestones:[],sections}).sections.timeline.state).toBe("fresh");expect(serializeClientServiceSection({state:"unavailable",generatedAt:M009_TEST_NOW.toISOString(),data:[{label:"secret"}]}as never)).not.toHaveProperty("data")})});
