import{describe,expect,it}from"vitest";import{getProcessStatusCopy}from"@atlas/i18n";import{createProcessStatusAnalyticsEvent}from"@atlas/observability";describe("M010 i18n and telemetry",()=>{it("keeps ES and EN semantic keys aligned",()=>expect(Object.keys(getProcessStatusCopy("es")).sort()).toEqual(Object.keys(getProcessStatusCopy("en")).sort()));it("allows only health metadata",()=>{expect(createProcessStatusAnalyticsEvent("process_status_health_detail",{outcome:"ok",locale:"en"})).toEqual({event:"process_status_health_detail",outcome:"ok",locale:"en"});expect(()=>createProcessStatusAnalyticsEvent("process_status_health_detail",{outcome:"ok",serviceRef:"csr1_secret"}as never)).toThrow()})});
