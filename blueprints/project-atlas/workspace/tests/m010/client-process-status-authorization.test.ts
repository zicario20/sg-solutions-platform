import{describe,expect,it}from"vitest";import{isProcessRootAuthorized}from"@atlas/client-process-status";const snapshot={accountId:"account",accountStatus:"active",sessionStatus:"active",sessionExpiresAt:"2026-08-24T00:00:00Z",context:{opaqueRef:"ctx"},authorizationEpoch:7,policyEpoch:9,assurance:"aal2"}as any;const root={ownerAccountId:"account",ownerContextRef:"ctx",grant:{permission:"client.service.read",state:"active",authorizationEpoch:"7",policyEpoch:"9",minimumAssurance:"aal1"},entitlement:{state:"active",version:"ent.v1",authorizationEpoch:"7",policyEpoch:"9"}}as any;describe("M010 authorization",()=>{it("requires explicit permission and active independent entitlement",()=>{expect(isProcessRootAuthorized(snapshot,root,new Date("2026-08-23T00:00:00Z"))).toBe(true);expect(isProcessRootAuthorized(snapshot,{...root,grant:{...root.grant,permission:"client.other"}},new Date("2026-08-23T00:00:00Z"))).toBe(false);expect(isProcessRootAuthorized(snapshot,{...root,entitlement:{...root.entitlement,state:"revoked"}},new Date("2026-08-23T00:00:00Z"))).toBe(false)});it("fails closed on expired entitlement or epoch drift",()=>{expect(isProcessRootAuthorized(snapshot,{...root,entitlement:{...root.entitlement,expiresAt:"2026-08-22T00:00:00Z"}},new Date("2026-08-23T00:00:00Z"))).toBe(false);expect(isProcessRootAuthorized(snapshot,{...root,entitlement:{...root.entitlement,policyEpoch:"8"}},new Date("2026-08-23T00:00:00Z"))).toBe(false)})});
