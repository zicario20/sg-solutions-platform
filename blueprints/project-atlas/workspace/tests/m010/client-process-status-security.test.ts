import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const read=(path:string)=>readFileSync(resolve(import.meta.dirname,"../..",path),"utf8");describe("M010 security boundary",()=>{it("creates no schema writer job provider call or browser storage",()=>{const sources=[read("packages/client-process-status/src/query-service.ts"),read("apps/app/src/lib/process-status/configured-runtime.ts"),read("packages/ui/src/process-status/ProcessStatusView.tsx")].join("\n");expect(sources).not.toMatch(/create table|migration|inngest|stripe|localStorage|sessionStorage|service_role/i)});it("uses only csr1 references and private HTTP",()=>{const http=read("apps/app/src/lib/process-status/http.ts");expect(http).toContain("CLIENT_SERVICE_REF_PATTERN");expect(http).toContain("private, no-store")})});
