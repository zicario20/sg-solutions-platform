import{describe,expect,it}from"vitest";import{parseClientProcessDetailDto,parseClientProcessLandingDto}from"@atlas/client-process-status";const ref="csr1_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",detail={schemaVersion:"m010.detail.v1",availability:"fresh",context:{type:"personal",label:"Personal"},service:{serviceRef:ref,label:"Service"},status:{code:"in_progress"},nextAction:{type:"due_task",label:"Complete task",responsibleParty:"client",routeKey:"status"},blockers:[{code:"task.blocked",label:"Task blocked",effect:"action_required",responsibleParty:"client"}],milestones:[{label:"Review",state:"blocked"}],timeline:{state:"fresh",items:[{eventRef:"pev1_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",code:"step_completed",copyKey:"timeline.step_completed",actorCategory:"sg_solutions",occurredAt:"2026-08-23T01:00:00Z"}],hasMore:false},sections:{tasks:{state:"fresh",asOf:"2026-08-23T01:00:00Z",items:[{label:"Task"}]}}};describe("M010 deep contracts",()=>{it("reconstructs every nested DTO and strips prototype/extraneous ownership",()=>{const parsed=parseClientProcessDetailDto(detail);expect(parsed).toEqual(detail);expect(Object.isFrozen(parsed.timeline?.items)).toBe(true)});it.each([["status","internalStatus"],["action","href"],["blocker","employeeNote"],["milestone","internalId"],["timeline","signedUrl"],["section","accountId"],["item","amount"]])("rejects undeclared nested field in %s",(_,key)=>{const candidate=structuredClone(detail)as any;if(_==="status")candidate.status[key]=1;if(_==="action")candidate.nextAction[key]=1;if(_==="blocker")candidate.blockers[0][key]=1;if(_==="milestone")candidate.milestones[0][key]=1;if(_==="timeline")candidate.timeline.items[0][key]=1;if(_==="section")candidate.sections.tasks[key]=1;if(_==="item")candidate.sections.tasks.items[0][key]=1;expect(()=>parseClientProcessDetailDto(candidate)).toThrow("PROCESS_STATUS_CONTRACT_INVALID")});it("rejects raw codes, dates, routes and cursor shapes",()=>{expect(()=>parseClientProcessDetailDto({...detail,timeline:{...detail.timeline,items:[{...detail.timeline.items[0],copyKey:"internal.raw"}]}})).toThrow();expect(()=>parseClientProcessDetailDto({...detail,lastConfirmedAt:"tomorrow"})).toThrow();expect(()=>parseClientProcessLandingDto({schemaVersion:"m010.landing.v1",availability:"fresh",context:{type:"personal",label:"Personal"},choices:[],hasMore:true,cursor:"12"})).toThrow()})});
