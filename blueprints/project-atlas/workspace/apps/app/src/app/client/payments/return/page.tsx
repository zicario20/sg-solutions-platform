import { ClientPortalShell } from "@atlas/ui";
import { requireDashboardPageContext } from "../../../../lib/dashboard/page-context.ts";
export default async function Page(){const {locale}=await requireDashboardPageContext();const copy=locale==="es"?{title:"Estamos verificando tu pago",body:"La página de retorno no confirma pagos. El estado se actualizará únicamente después de una verificación segura del proveedor.",action:"Volver a pagos"}:{title:"We are verifying your payment",body:"The return page does not confirm payments. Status updates only after secure provider verification.",action:"Return to payments"};return <ClientPortalShell locale={locale} activeRoute="payments"><section aria-labelledby="payment-return-title"><p>SG Solutions</p><h1 id="payment-return-title">{copy.title}</h1><p>{copy.body}</p><a className="portal-cta" href="/client/payments">{copy.action}</a></section></ClientPortalShell>}
