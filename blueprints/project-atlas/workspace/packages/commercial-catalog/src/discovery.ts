import { CatalogControlError } from "./governance.ts";import type {  ServiceCatalogLocale,  ServiceDefinition,  ServiceSurface,  ServiceVersion,} from "./service-registry.ts";export type ServiceDiscoveryDocument = Readonly<{  code: string;  categoryCode: string;  canonicalPath: string;  title: string;  description: string;  availability: "available" | "limited" | "waitlist";  locale: ServiceCatalogLocale;}>;export type ServiceSurfaceProjectionRequest = Readonly<{  surface: ServiceSurface;  locale: ServiceCatalogLocale;  authorized: boolean;  purpose: "public_discovery" | "client_service_detail" | "catalog_administration";  clientHasServiceGrant: boolean;}>;export function buildServiceDiscoveryIndex(  records: readonly Readonly<{ definition: ServiceDefinition; version: ServiceVersion }>[],): readonly ServiceDiscoveryDocument[] {  const documents: ServiceDiscoveryDocument[] = [];  for (const record of records) {    const { definition, version } = record;    const availability = definition.availability.status;    if (      version.publicationStatus !== "published" ||      !definition.surfaces.includes("public") ||      (availability !== "available" && availability !== "limited" && availability !== "waitlist") ||      version.seo === null    )      continue;    documents.push(      Object.freeze({        code: definition.code,        categoryCode: definition.categoryCode,        canonicalPath: version.seo.canonicalPath,        title: version.seo.title,        description: version.seo.description,        availability,        locale: "en",      }),    );  }  return Object.freeze(documents);}export function projectServiceForSurface(  definition: ServiceDefinition,  version: ServiceVersion,  request: ServiceSurfaceProjectionRequest,): Readonly<Record<string, unknown>> {  if (!request.authorized) throw new CatalogControlError("authorization required");  const translation = version.translations[request.locale];  if (request.surface === "public") {    if (request.purpose !== "public_discovery") throw new CatalogControlError("public purpose required");    return Object.freeze({      code: definition.code,      categoryCode: definition.categoryCode,      name: translation.name,      summary: translation.summary,      benefits: translation.benefits,      limitations: translation.limitations,      ctaLabel: translation.ctaLabel,      availability: definition.availability.status,      canonicalPath: version.seo?.canonicalPath ?? null,      duration: version.durationProfile === null        ? null        : {            type: version.durationProfile.type,            unit: version.durationProfile.unit,            minimum: version.durationProfile.minimum,            maximum: version.durationProfile.maximum,            confidence: version.durationProfile.confidence,          },    });  }  if (request.surface === "client") {    if (request.purpose !== "client_service_detail" || !request.clientHasServiceGrant)      throw new CatalogControlError("client service grant required");    return Object.freeze({      code: definition.code,      name: translation.name,      summary: translation.summary,      limitations: translation.limitations,      availability: definition.availability.status,      documentRequirementSetReference: version.documentRequirementSetReference,      disclosureSetReference: version.disclosureSetReference,      durationProfile: version.durationProfile,    });  }  if (request.purpose !== "catalog_administration") throw new CatalogControlError("admin purpose required");  return Object.freeze({    definition,    version,    publicationReadiness: definition.availability.status === "unknown" ? "blocked" : "review_required",  });}