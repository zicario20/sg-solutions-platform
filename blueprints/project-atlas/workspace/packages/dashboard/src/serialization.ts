import type { DashboardDto, DashboardSection, DashboardSectionState } from "./contracts.ts";
const safeStates = new Set<DashboardSectionState>(["fresh", "empty", "stale", "unavailable", "suppressed"]);
function normalizeSection<T>(section: DashboardSection<T>): DashboardSection<T> { const state = safeStates.has(section.state) ? section.state : "unavailable"; return Object.freeze({ state, ...(typeof section.asOf === "string" ? { asOf: section.asOf } : {}), ...(state === section.state && section.safeReason ? { safeReason: section.safeReason } : state === "unavailable" ? { safeReason: "source_unavailable" as const } : {}), ...(state === "fresh" && section.data !== undefined ? { data: section.data } : {}) }); }
/** Final allowlist after provider/cache output and immediately before HTTP serialization. */
export function sanitizeDashboardDtoForSerialization(dto: DashboardDto): DashboardDto { return Object.freeze({ locale: dto.locale, ...(dto.greetingName ? { greetingName: dto.greetingName } : {}), context: Object.freeze({ type: dto.context.type, selectedOpaqueRef: dto.context.selectedOpaqueRef, options: Object.freeze(dto.context.options.map((option) => Object.freeze({ type: option.type, opaqueRef: option.opaqueRef, label: option.label }))) }), priority: dto.priority, importantAlerts: normalizeSection(dto.importantAlerts), security: normalizeSection(dto.security), services: normalizeSection(dto.services), tasks: normalizeSection(dto.tasks), documents: normalizeSection(dto.documents), appointments: normalizeSection(dto.appointments), payments: normalizeSection(dto.payments), messages: normalizeSection(dto.messages), notifications: normalizeSection(dto.notifications), help: normalizeSection(dto.help) }); }
