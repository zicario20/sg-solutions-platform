import { boolean, integer, pgTable, text, timestamp, uuid } from "drizzle-orm/pg-core";

export const technicalRoadmaps = pgTable("m100_technical_roadmaps", { id: uuid("id").primaryKey(), code: text("code").notNull().unique(), title: text("title").notNull(), status: text("status").notNull(), active: boolean("active").notNull().default(false), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull(), updatedAt: timestamp("updated_at", { withTimezone: true, mode: "date" }).notNull() });
export const technicalRoadmapVersions = pgTable("m100_technical_roadmap_versions", { id: uuid("id").primaryKey(), roadmapId: uuid("roadmap_id").notNull().references(() => technicalRoadmaps.id), code: text("code").notNull().unique(), versionNumber: integer("version_number").notNull(), changeSummary: text("change_summary").notNull(), status: text("status").notNull(), approved: boolean("approved").notNull().default(false), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull() });
export const technicalInitiatives = pgTable("m100_technical_initiatives", { id: uuid("id").primaryKey(), roadmapVersionId: uuid("roadmap_version_id").notNull().references(() => technicalRoadmapVersions.id), code: text("code").notNull().unique(), horizon: text("horizon").notNull(), ownerModuleReferences: text("owner_module_references").array().notNull(), dependencyReferences: text("dependency_references").array().notNull(), evidenceReferences: text("evidence_references").array().notNull(), status: text("status").notNull(), progress: text("progress").notNull(), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull() });
export const technicalMilestones = pgTable("m100_technical_milestones", { id: uuid("id").primaryKey(), initiativeId: uuid("initiative_id").notNull().references(() => technicalInitiatives.id), code: text("code").notNull().unique(), definitionOfDoneReferences: text("definition_of_done_references").array().notNull(), evidenceReferences: text("evidence_references").array().notNull(), status: text("status").notNull(), complete: boolean("complete").notNull().default(false), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull() });
export const technicalRisks = pgTable("m100_technical_risks", { id: uuid("id").primaryKey(), roadmapVersionId: uuid("roadmap_version_id").notNull().references(() => technicalRoadmapVersions.id), code: text("code").notNull().unique(), severity: text("severity").notNull(), category: text("category").notNull(), evidenceReferences: text("evidence_references").array().notNull(), status: text("status").notNull(), accepted: boolean("accepted").notNull().default(false), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull() });
export const technicalDecisionGates = pgTable("m100_technical_decision_gates", { id: uuid("id").primaryKey(), roadmapVersionId: uuid("roadmap_version_id").notNull().references(() => technicalRoadmapVersions.id), code: text("code").notNull().unique(), decisionReference: text("decision_reference").notNull(), affectedInitiativeReferences: text("affected_initiative_references").array().notNull(), status: text("status").notNull(), decisionRecorded: boolean("decision_recorded").notNull().default(false), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull() });
export const technicalRoadmapReviews = pgTable("m100_technical_roadmap_reviews", { id: uuid("id").primaryKey(), roadmapVersionId: uuid("roadmap_version_id").notNull().references(() => technicalRoadmapVersions.id), code: text("code").notNull().unique(), evidenceReferences: text("evidence_references").array().notNull(), status: text("status").notNull(), reviewPerformed: boolean("review_performed").notNull().default(false), createdAt: timestamp("created_at", { withTimezone: true, mode: "date" }).notNull() });