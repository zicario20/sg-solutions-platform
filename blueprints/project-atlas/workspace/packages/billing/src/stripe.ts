import {createHmac,timingSafeEqual} from "node:crypto";
export function verifyStripeSignature(body:string,header:string|null,secret:string,now=Math.floor(Date.now()/1000)){if(!header||secret.length<16)return false;const entries=Object.fromEntries(header.split(",").map(x=>x.trim().split("=",2)).filter(x=>x.length===2));const timestamp=Number(entries.t),signature=entries.v1;if(!Number.isSafeInteger(timestamp)||!signature||Math.abs(now-timestamp)>300)return false;const expected=createHmac("sha256",secret).update(`${timestamp}.${body}`).digest("hex"),left=Buffer.from(expected,"hex"),right=Buffer.from(signature,"hex");return left.length===right.length&&timingSafeEqual(left,right)}
