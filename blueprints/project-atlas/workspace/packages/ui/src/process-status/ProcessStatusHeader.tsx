import type{ClientProcessDetailDto,ProcessLocale}from"@atlas/client-process-status";import{getProcessStatusCopy}from"@atlas/i18n";export function ProcessStatusHeader({locale,dto}:{locale:ProcessLocale;dto:ClientProcessDetailDto}){const copy=getProcessStatusCopy(locale);return<header className="m010-detail-header"><a href="/client/status">← {copy.back}</a><span>{dto.context.label}</span><h1>{dto.service.label}</h1>{dto.service.instanceLabel?<p>{dto.service.instanceLabel}</p>:null}{dto.status?<p className="m010-status-badge">{copy.states[dto.status.code]}</p>:null}{dto.lastConfirmedAt?<p>{copy.lastConfirmed}: <time dateTime={dto.lastConfirmedAt}>{new Intl.DateTimeFormat(locale,{dateStyle:"medium"}).format(new Date(dto.lastConfirmedAt))}</time></p>:null}</header>}
