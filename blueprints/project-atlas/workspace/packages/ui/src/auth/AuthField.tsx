export function AuthField({ label, type = "text", name, autoComplete, error }: { label: string; type?: string; name?: string; autoComplete?: string; error?: string }) { const stableName = name ?? (type === "email" ? "email" : type === "password" ? "password" : "value"); const id = `auth-${stableName}`; const errorId = `${id}-error`; return <p className="auth-field"><label htmlFor={id}>{label}</label><input id={id} name={stableName} type={type} autoComplete={autoComplete} aria-invalid={Boolean(error)} aria-describedby={error ? errorId : undefined} />{error ? <span id={errorId} role="alert">{error}</span> : null}</p>; }
export function authFormAttributes(flow: "sign-in" | "register" | "recovery" | "reset") { return { action: flow === "sign-in" ? "/api/auth/login" : flow === "register" ? "/api/auth/register" : flow === "reset" ? "/api/auth/reset" : "/api/auth/recovery", method: "post" as const }; }
