import{secureMessagingCopy}from"@atlas/i18n";import type{DashboardLocale}from"@atlas/dashboard";export function SecureMessagingPortal({locale,state}:{locale:DashboardLocale;state:"unavailable"|"empty"}){const copy=secureMessagingCopy[locale];return <section className="secure-messaging" aria-labelledby="messages-title"><header><p>Secure portal</p><h1 id="messages-title">{copy.title}</h1><p>{copy.intro}</p></header><div role="status"><p>{state==="unavailable"?copy.unavailable:copy.empty}</p></div></section>}
