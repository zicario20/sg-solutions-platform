# SG Solutions Platform

- Owner: SG Solutions Product Owner
- Status: Architecture baseline; code implementation not started
- Update rule: revise after every completed build task and link the governing PCR

SG Solutions Platform is one professional web application for selling and operating SG Solutions services. `Project Atlas` is an internal project name. Read `PRODUCT_DEFINITION.md`, `PROJECT_CONTEXT.md`, `PROJECT_STATE.md`, `AGENTS.md`, and the relevant module PRD before changing the repository.

Current work is Phase 0 documentary review. Implementation is not started or authorized; task and epic artifacts do not grant permission to execute.

The former E1–E3 blueprint, queue and epics are preserved under `../archive/pre-roadmap-2026-08-02/` as superseded history. The active entry point is `../blueprint.md`; no executable task queue exists.
