# Required credentials / integrations (placeholders only)

Do not invent IDs. Replace with your real credentials and IDs in n8n.

## Required or optional

- Qwen3:14B inference endpoint credential:
  - API URL
  - API key / token (if required)
  - model name (qwen3-14b)
- Session store (optional) REST credential
- Calendar system credential or API key (Google/Microsoft/CalDav/etc)
- CRM or lead database credential
- Optional queue or email/chat tool for human handoff
- Logging endpoint for event archive

## Safe note on IDs

Any value not in your real environment must remain:
- `REPLACE_WITH_...`
- to avoid accidental wrong references.

